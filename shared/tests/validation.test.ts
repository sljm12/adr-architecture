import { describe, expect, it } from 'vitest'; import { diagramDocumentSchema } from '../src/index';
describe('document validation',()=>{it('rejects invalid relationships with actionable paths',()=>{const result=diagramDocumentSchema.safeParse({id:'00000000-0000-0000-0000-000000000001',name:'x',status:'active',createdAt:'x',updatedAt:'x',trashedAt:null,components:[],relationships:[{id:'00000000-0000-0000-0000-000000000002',diagramId:'00000000-0000-0000-0000-000000000001',sourceComponentId:'00000000-0000-0000-0000-000000000003',targetComponentId:'00000000-0000-0000-0000-000000000004',direction:'directed',label:null,createdAt:'x',updatedAt:'x'}]}); expect(result.success).toBe(false); if(!result.success)expect(result.error.issues[0].path.join('.')).toContain('relationships');});});
