import {describe,expect,it} from 'vitest'; import {toReactFlow,fromReactFlow,nearestHandle} from '../src/adapters/react-flow/diagram-adapter'; const d:any={id:'d',name:'x',status:'active',createdAt:'n',updatedAt:'n',trashedAt:null,components:[{id:'a',diagramId:'d',name:'API',position:{x:1,y:2}},{id:'b',diagramId:'d',name:'DB',position:{x:100,y:2}}],relationships:[{id:'r1',diagramId:'d',sourceComponentId:'a',targetComponentId:'b',direction:'directed',label:'one'},{id:'r2',diagramId:'d',name:'reverse',sourceComponentId:'b',targetComponentId:'a',direction:'directed',label:'two'}]}; describe('React Flow adapter',()=>{it('round trips IDs and positions',()=>{const visual=toReactFlow(d);expect(visual.nodes[0].id).toBe('a');expect(fromReactFlow(d,[{...visual.nodes[0],position:{x:9,y:8}} as any]).components[0].id).toBe('a');});it('selects the nearest directional handles',()=>{expect(nearestHandle({x:0,y:0},{x:100,y:10})).toEqual({source:'right',target:'left'});expect(nearestHandle({x:0,y:0},{x:10,y:100})).toEqual({source:'bottom',target:'top'});});it('fans out relationships in the same and opposite directions',()=>{const edges=toReactFlow(d).edges;expect(edges[0].data?.offset).not.toBe(edges[1].data?.offset);expect(Math.abs(edges[0].data?.offset as number)).toBe(20);expect(Math.abs(edges[1].data?.offset as number)).toBe(20);const parallel=toReactFlow({...d,relationships:[d.relationships[0],{...d.relationships[0],id:'r3'}]}).edges;expect(parallel[0].data?.offset).toBe(-20);expect(parallel[1].data?.offset).toBe(20);});});
