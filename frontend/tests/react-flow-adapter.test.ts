import {describe,expect,it} from 'vitest'; import {toReactFlow,fromReactFlow} from '../src/adapters/react-flow/diagram-adapter'; const d:any={id:'d',name:'x',status:'active',createdAt:'n',updatedAt:'n',trashedAt:null,components:[{id:'c',diagramId:'d',name:'API',position:{x:1,y:2}}],relationships:[]}; describe('React Flow adapter',()=>it('round trips IDs and positions',()=>{const visual=toReactFlow(d);expect(visual.nodes[0].id).toBe('c');expect(fromReactFlow(d,[{...visual.nodes[0],position:{x:9,y:8}} as any]).components[0].id).toBe('c');}));
