import {DiagramToolbar} from './DiagramToolbar'; import {DiagramCanvas} from './DiagramCanvas'; import {useDiagramStore} from '../state/diagram-store';
export function DiagramWorkspace(){const document=useDiagramStore(s=>s.document);return <div className="app-shell"><nav className="global-nav" aria-label="Global navigation"><a className="brand" href="/" aria-label="ADR Diagram home"><span className="brand-mark">◇</span> ADR Diagram</a><div className="global-links"><a href="#diagrams">Diagrams</a><a href="#decisions">Decisions</a><a href="#help">Help</a></div><button className="nav-utility" type="button">Workspace</button></nav><div className="sub-nav"><div><span className="eyebrow">Architecture workspace</span><h1>{document?.name??'Diagram studio'}</h1></div><div className="sub-nav-actions"><span className="quiet-note">Single-user workspace</span><button className="primary-pill" type="button" onClick={()=>document?.id&&window.location.reload()}>New view</button></div></div><div className="workspace"><aside className="workspace-sidebar"><div className="sidebar-intro"><span className="eyebrow">Your artifacts</span><h2>Make structure visible.</h2><p>Shape a clear architecture map, then let every decision point back to it.</p></div><div className="sidebar-card"><span className="card-kicker">Current diagram</span><strong>{document?.name??'No diagram yet'}</strong><span className="card-meta">{document?`${document.components.length} components · ${document.relationships.length} relationships`:'Create a diagram to begin'}</span></div><div className="sidebar-card sidebar-card-dark"><span className="card-kicker">Keyboard tip</span><strong>Keep moving</strong><span className="card-meta">Drag components to refine the story. Changes save automatically.</span></div></aside><section className="editor-area"><DiagramToolbar/><DiagramCanvas/></section></div></div>;}
