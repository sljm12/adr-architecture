import {useState} from 'react'; import {useDiagramStore} from '../state/diagram-store'; import {SaveStatus} from './SaveStatus'; export function DiagramToolbar(){const [name,setName]=useState('');const [componentName,setComponentName]=useState('');const [source,setSource]=useState('');const [target,setTarget]=useState('');const [label,setLabel]=useState('');const [direction,setDirection]=useState<'directed'|'undirected'>('directed');const document=useDiagramStore(s=>s.document);const create=useDiagramStore(s=>s.create);const addComponent=useDiagramStore(s=>s.addComponent);const addRelationship=useDiagramStore(s=>s.addRelationship);return <header><section className="toolbar-group"><label htmlFor="diagram-name">Diagram name</label><input id="diagram-name" value={name} onChange={e=>setName(e.target.value)}/><button onClick={()=>void create(name)} disabled={!name.trim()}>Create diagram</button></section>{document&&<><section className="toolbar-group"><label htmlFor="component-name">Component name</label><input id="component-name" value={componentName} onChange={e=>setComponentName(e.target.value)} placeholder="e.g. API"/><button onClick={()=>{addComponent(componentName);setComponentName('')}} disabled={!componentName.trim()}>Add component</button></section><section className="toolbar-group"><label htmlFor="relationship-source">Relationship</label><select id="relationship-source" value={source} onChange={e=>setSource(e.target.value)}><option value="">From…</option>{document.components.map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select><select aria-label="Relationship target" value={target} onChange={e=>setTarget(e.target.value)}><option value="">To…</option>{document.components.map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select><input aria-label="Relationship label" value={label} onChange={e=>setLabel(e.target.value)} placeholder="Label (optional)"/><select aria-label="Relationship direction" value={direction} onChange={e=>setDirection(e.target.value as 'directed'|'undirected')}><option value="directed">Directed</option><option value="undirected">Undirected</option></select><button onClick={()=>{addRelationship(source,target,label,direction);setLabel('')}} disabled={!source||!target||source===target}>Connect</button></section></>}<SaveStatus/></header>;}
