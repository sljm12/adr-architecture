import {useCallback} from 'react'; import {ReactFlow,Background,Controls} from '@xyflow/react'; import '@xyflow/react/dist/style.css'; import {toReactFlow} from '../adapters/react-flow/diagram-adapter'; import {useDiagramStore} from '../state/diagram-store'; export function DiagramCanvas(){const d=useDiagramStore(s=>s.document);const update=useDiagramStore(s=>s.update);const visual=d?toReactFlow(d):{nodes:[],edges:[]};const onNodeDragStop=useCallback((_:unknown,node:any)=>update(x=>({...x,components:x.components.map(c=>c.id===node.id?{...c,position:node.position}:c)})),[update]);return <main aria-label="Architecture diagram canvas"><ReactFlow key={d?.id??'empty'} nodes={visual.nodes} edges={visual.edges} onNodesChange={()=>undefined} onEdgesChange={()=>undefined} onNodeDragStop={onNodeDragStop} fitView><Background/><Controls/></ReactFlow></main>;}
