import {BaseEdge,type EdgeProps} from '@xyflow/react';
export function RelationshipEdge({sourceX,sourceY,targetX,targetY,markerEnd,label,data}:EdgeProps<{offset?:number}>){const midpointX=(sourceX+targetX)/2;const midpointY=(sourceY+targetY)/2;const dx=targetX-sourceX;const dy=targetY-sourceY;const distance=Math.max(Math.hypot(dx,dy),1);const offset=data?.offset??0;const controlX=midpointX-(dy/distance)*offset;const controlY=midpointY+(dx/distance)*offset;const path=`M ${sourceX} ${sourceY} Q ${controlX} ${controlY} ${targetX} ${targetY}`;const labelX=(sourceX+2*controlX+targetX)/4;const labelY=(sourceY+2*controlY+targetY)/4;return <><BaseEdge path={path} markerEnd={markerEnd}/>{label&&<text x={labelX} y={labelY} textAnchor="middle" dominantBaseline="middle" className="relationship-label">{label}</text>}</>;}
