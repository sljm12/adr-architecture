import { useDiagramStore } from '../state/diagram-store'; export function SaveStatus() { const { status, error } = useDiagramStore(); return <p className={`save-status save-${status}`} role="status" aria-live="polite" aria-atomic="true"><span className="status-dot" aria-hidden="true" />{status === 'saving' ? 'Saving…' : status === 'saved' ? 'Saved automatically' : status === 'failed' ? `Save failed: ${error}` : 'Ready'}</p>; }
