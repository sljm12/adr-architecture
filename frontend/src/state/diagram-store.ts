import {create} from 'zustand'; import type {DiagramDocument} from '../../../../shared/src/index'; import {diagramClient} from '../api/diagram-client';
type SaveStatus='idle'|'saving'|'saved'|'failed'; type State={document:DiagramDocument|null;status:SaveStatus;error:string|null;open:(d:DiagramDocument)=>void;create:(name:string)=>Promise<void>;update:(fn:(d:DiagramDocument)=>DiagramDocument)=>void;addComponent:(name:string)=>void;addRelationship:(sourceComponentId:string,targetComponentId:string,label:string,direction:'directed'|'undirected')=>void;save:()=>Promise<void>}; let timer:ReturnType<typeof setTimeout>|undefined;
const now=()=>new Date().toISOString();
export const useDiagramStore=create<State>((set,get)=>({document:null,status:'idle',error:null,open:d=>set({document:d,status:'saved',error:null}),create:async name=>{const d=await diagramClient.create(name);set({document:d,status:'saved',error:null});},update:fn=>{const d=get().document;if(!d)return;set({document:fn(d),status:'saving',error:null});clearTimeout(timer);timer=setTimeout(()=>void get().save(),300);},addComponent:name=>{const d=get().document;if(!d||!name.trim())return;const timestamp=now();get().update(x=>({...x,components:[...x.components,{id:crypto.randomUUID(),diagramId:x.id,name:name.trim(),description:null,type:null,position:{x:80+x.components.length*180,y:100+(x.components.length%3)*120},createdAt:timestamp,updatedAt:timestamp}]}));},addRelationship:(sourceComponentId,targetComponentId,label,direction)=>{const d=get().document;if(!d||sourceComponentId===targetComponentId)return;const timestamp=now();get().update(x=>({...x,relationships:[...x.relationships,{id:crypto.randomUUID(),diagramId:x.id,sourceComponentId,targetComponentId,label:label.trim()||null,direction,createdAt:timestamp,updatedAt:timestamp}]}));},save:async()=>{const d=get().document;if(!d)return;set({status:'saving'});try{const saved=await diagramClient.save(d);set({document:saved,status:'saved',error:null});}catch(e){set({status:'failed',error:e instanceof Error?e.message:'Save failed'});}}}));
