import { describe,expect,it } from 'vitest'; import fs from 'node:fs'; describe('OpenAPI contract',()=>it('declares diagram endpoints and saved-document summaries',()=>{const s=fs.readFileSync('specs/001-software-architecture-diagrams/contracts/openapi.yaml','utf8');expect(s).toContain('/diagrams/{diagramId}:');expect(s).toContain('DiagramDocument');expect(s).toContain('List active saved-diagram summaries');expect(s).toContain('DiagramSummaryList');}));
